# Estudos sobre o relevo de Valdeorras

Probas para mostrar un mapa co relevo de Valdeorras usando Three.js

Para probalo, antes de nada executa no terminal:

```bash
npm install
```

e despois

```bash
npx vite
```
